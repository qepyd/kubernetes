对kubernetes中所有的资源对象进行备份、恢复。  
为什么需要备份:
```
  当删除k8s中的某一个资源对象(例如:deploy/myapp)后,如何恢复
     你得找到创建时的manifests或命令(kubectl、helm)
     万一找不到呢
  
  当删除k8s中的某一个资源对象(例如:ns/dev）,如何恢复里面的相关资源对象(那可就多个)呢
     你得找到创建各资源对象时的manifests或命令(kubectl、helm命令行创建)
     万一找不到呢

  当出现故障后
     需要的是快速准备的恢复
```

