## 1.创建client.binbinrbd用户,并做相应的授权
```
## 创建 client.binbinrbd 用户,对 mon 有只读权限,对 osd 中其 rbd-binbin-project-data 存储池有rwx权限
ceph auth get client.binbinrbd
ceph auth add client.binbinrbd  mon 'allow r' osd 'allow rwx pool=rbd-binbin-project-data'
ceph auth get client.binbinrbd

## 导出client.binbinrbd用户的key到本机 PS：后面要给到binbin项目的运维人员
ceph auth print-key client.binbinrbd   # 屏幕上打印
ceph auth print-key client.binbinrbd -o /tmp/ceph.client.binbinrbd.secret
ls -l /tmp/ceph.client.binbinrbd.secret

admin@ceph-mon01:~$ cat /tmp/ceph.client.binbinrbd.secret
AQA9/ZNoqB5eChAAVZIW9/bSGQK+uQQ5nwnU9A==


## 导出 client.binbinrbd 用户的keyring到本机, PS：后面要给到binbin项目的运维人员
ceph auth get client.binbinrbd         # 屏幕上打印 
ceph auth get client.binbinrbd -o /tmp/ceph.client.binbinrbd.keyring
ls -l /tmp/ceph.client.binbinrbd.keyring

admin@ceph-mon01:~$ cat /tmp/ceph.client.binbinrbd.keyring 
[client.binbinrbd]
	key = AQA9/ZNoqB5eChAAVZIW9/bSGQK+uQQ5nwnU9A==
	caps mon = "allow r"
	caps osd = "allow rwx pool=rbd-binbin-project-data"
```

## 2.告诉binbin项目的运维人员
```
其binbin项目的各应用,在使用ceph的rbd时：
  A：其monitory的连接地址为：
     172.31.8.201:6789
     172.31.8.202:6789
     172.31.8.203:6789

  B：用户名：
     binbinrbd

  C：密钥和密钥环: 
     ceph.client.binbinrbd.secret
     ceph.client.binbinrbd.keyring

  D：存储池：rbd-binbin-project-data

  E：至于各应用要用到image,用到时再来沟通,再创建,再交付
```
