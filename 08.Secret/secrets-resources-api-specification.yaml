apiVersion: v1
kind: Secret
metadata:
  namespace: <string>
  name: <string>
  labels: <map[string]string>
  annotations: <map[string]string>

####### 不可变
#  默认值为nil
#    此资源对象没有此字段,此资源对象是可变的。
#  值为false
#    此资源对象拥有此字段,此资源对象是可变的。
#  值为true
#    此资源对象拥有此字段,此资源对象是不可变的。
immutable: <boolean>

####### 类型
#  https://kubernetes.io/zh-cn/docs/concepts/configuration/secret/#secret-types
#  默认值：Opaque
#  可设值：kubernetes.io/service-account-token  
#          kubernetes.io/dockercfg
#          kubernetes.io/dockerconfigjson
#          kubernetes.io/basic-auth
#          kubernetes.io/ssh-auth
#          kubernetes.io/tls
#          bootstrap.kubernetes.io/token
type: <string>

####### 存放键值对的字段，常用data字段。
## 此字段中的键其value得base64加密后填写(不然拒绝此资源对象创建)。
#  在describe此资源对象时,此字段下其键的value是不显示的。
#  查看此资源对象的在线manifests时,是有此字段的。
data: <map[string]string>

## 此字段中的键其value不需要base64加密后填写。
#  在describe此资源对象时,此字段下其键的value是不显示的。
#  查看此资源对象的在线manifests时,是没有此字段的(键值对会放在data字段中)
#  所以: data、stringData中的键冲突时,不影响此资源对象的创建,会以stringData中的键值对为准。
stringData: <map[string]string>
