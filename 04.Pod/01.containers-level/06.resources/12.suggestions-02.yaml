apiVersion: v1
kind: Pod
metadata:
  namespace: lili
  name: suggestions-02 
  labels:
    app: suggestions-02 
spec:
  containers:
  - name: myapp01
    image: swr.cn-north-1.myhuaweicloud.com/library/nginx:1.16
    resources:
      # 不显示定义requests,会根据用limits中的进行填充)
      limits:
        cpu: "100m"
        memory: "100Mi"

