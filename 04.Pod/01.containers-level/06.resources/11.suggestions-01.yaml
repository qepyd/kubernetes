apiVersion: v1
kind: Pod
metadata:
  namespace: lili
  name: suggestions-01 
  labels:
    app: suggestions-01 
spec:
  containers:
  - name: myapp01
    image: swr.cn-north-1.myhuaweicloud.com/library/nginx:1.16
    resources:
      # 若显示定义,请根据limits中的保持一模一样(我们知道若没有requests,会用limits中的进行填充)
      requests:
        cpu: "100m"
        memory: "100Mi"
      limits:
        cpu: "100m"
        memory: "100Mi"

