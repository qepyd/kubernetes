apiVersion: v1
kind: LimitRange
metadata:
  namespace: lili
  name: mem-limit-range
spec:
  limits:
    - type: Container
      # 支持在线更改
      defaultRequest:
        memory: 256Mi
      # 支持在线更改
      default:
        memory: 512Mi
